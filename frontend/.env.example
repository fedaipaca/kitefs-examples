# Copy this file to .env and set the backend URL if it differs from the default.
# Vite reads variables prefixed with VITE_ and exposes them to the browser bundle.

# URL of the FastAPI backend. Defaults to http://localhost:8000 when not set.
# VITE_API_BASE_URL=http://localhost:8000
